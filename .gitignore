formai_env/
__pycache__/
*.pyc
.DS_Store

# Xcode / iOS app
DerivedData/
build/
*.xcuserstate
**/xcuserdata/
*.xcscmblueprint
*.xccheckout

# CocoaPods — restore with `pod install`. The Pods/ binaries (MediaPipe is
# ~280MB incl. a 169MB lib) exceed GitHub's 100MB file limit, so don't commit.
Pods/
